const CACHE='biicode-v12';
const APP=['./','./index.html','./manifest.json','./qr.js','./verify.html'];
const QR_INJECT='<script src="https://cdnjs.cloudflare.com/ajax/libs/qrcodejs/1.0.0/qrcode.min.js"></script><script src="./qr.js?v=1"></script>';
function injectQR(response){return response.text().then(html=>{const headers=new Headers(response.headers);headers.delete('content-length');const out=html.replace(/<\/body>/i,QR_INJECT+'</body>');return new Response(out,{status:response.status,statusText:response.statusText,headers});});}
self.addEventListener('install',event=>{event.waitUntil(caches.open(CACHE).then(async cache=>{for(const url of APP){try{const r=await fetch(url,{cache:'no-store'});if(r.ok)await cache.put(url,r)}catch(e){}}}).then(()=>self.skipWaiting()))});
self.addEventListener('activate',event=>{event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(k=>k!==CACHE).map(k=>caches.delete(k)))).then(()=>self.clients.claim()))});
self.addEventListener('fetch',event=>{const u=new URL(event.request.url);if(event.request.method!=='GET'||u.origin!==location.origin)return;if(event.request.destination==='document'||u.pathname.endsWith('.html')){event.respondWith(fetch(event.request,{cache:'no-store'}).then(r=>{if(!r.ok)return r;const isIndex=u.pathname.endsWith('/index.html')||u.pathname==='/'||u.pathname.endsWith('/biicode');if(!isIndex)return r;return injectQR(r).then(out=>{caches.open(CACHE).then(c=>c.put(event.request,out.clone())).catch(()=>{});return out})}).catch(()=>caches.match(event.request).then(r=>r||caches.match('./index.html'))));return}event.respondWith(fetch(event.request).catch(()=>caches.match(event.request)))})
